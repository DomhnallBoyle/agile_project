<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="uk.ac.qub.csc3045cs2.csc3045cs2.mapper.AuthenticationMapper">

    <insert id="createUser" parameterType="User" useGeneratedKeys="true">
    	INSERT into User(firstName, lastName, email) values(#{fistName}, #{lastName}, #{email})
    </insert>
    
    <insert id="createAccount" parameterType="Account" useGeneratedKeys="true">
    	INSERT into Account(user_id, userName, passWord) values(#{user_id}, #{userName}, #{passWord})
    </insert>
    
    <select id="findUserByName" resultType=User">
    	SELECT * from User where firstName LIKE #{firstName} AND lastName LIKE #{lastName}
    </select>
</mapper>