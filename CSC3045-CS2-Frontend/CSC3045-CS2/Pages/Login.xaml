<Page x:Class="CSC3045_CS2.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CSC3045_CS2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Login">

    <StackPanel>
        <TextBlock Text="{Binding LoginTitle, FallbackValue='Login'}" HorizontalAlignment="Center" Margin="0,10,0,25"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <TextBlock Text="{Binding LoginUsernameLabel, FallbackValue='Username:'}" HorizontalAlignment="Right" Margin="0,0,0,15"/>
                <TextBlock Text="{Binding LoginPasswordLabel, FallbackValue='Password:'}" HorizontalAlignment="Right"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="12,0,25,0">
                <TextBox MaxLength="25" Margin="0,0,0,15"/>
                <PasswordBox MaxLength="25"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</Page>
